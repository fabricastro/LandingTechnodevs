---
import Starfield from "../components/starfield.astro";
import ScrollIndicator from "./ScrollIndicator.jsx";
---

<section class="relative h-[100dvh] lg:h-full lg:py-10 px-5 md:px-12 lg:px-12 xl:px-28 flex lg:flex-col items-center">
  <Starfield />
  <div id="splash-content" class="relative h-screen sm:grid-cols-2 flex items-center w-full max-w-[1280px] justify-center">
    <div
      class="flex flex-col items-center max-w-[40rem] gap-10 lg:gap-4 self-end sm:gap-4 sm:self-auto sm:justify-self-end custom-shadow z-20 h-full justify-center"
    >
      <div
        class="gradient-text text-center font-extrabold w-full tracking-tighter text-4xl md:text-7xl py-2"
      >
        <h1>
          Software Factory &
          <br class="hidden lg:block" /> Soluciones Digitales
        </h1>
      </div>
      <span class="text-center text-lg gradient-text">
        Desarrollamos sistemas a medida, plataformas SaaS y aplicaciones web escalables para potenciar negocios en la era digital.
      </span>
      <a href="https://wa.me/+5492646270803" target="_blank"
        class="relative w-[200px] h-[50px] rounded-full p-[2px] bg-gradient-to-r from-primaryColor to-secondary-200 bg-[length:200%_200%] bg-[0%_0%] hover:bg-[100%_0%] transition-all duration-200 ease-out"
      >
        <span
          class="flex items-center justify-center w-full h-full bg-[#03041f] text-white rounded-full"
        >
          Contáctanos
        </span>
      </a>
      
      <!-- Icono de scroll animado -->
      <ScrollIndicator client:load />
    </div>
  </div>
  <div class="hidden lg:block relative w-screen justify-center mb-[-42px] z-10">
    <svg
      data-name="Layer 1"
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 1200 120"
      preserveAspectRatio="none"
      class="w-full custom-shadow"
    >
      <path
        d="M0,0 L600,100 L1200,0 L1200,120 L0,120 Z"
        class="bg-blackPrimary fill-blackPrimary"
        filter="url(#dropShadowExternal)"></path>
    </svg>
  </div>
</section>

<noscript>
  <style>
    #splash-bg-fallback {
      display: block;
    }
  </style>
</noscript>

<style>
  .custom-shadow {
    filter: drop-shadow(0 0 20mm rgb(200, 200, 200, 0.4));
  }
  @keyframes float {
    0% {
      transform: translate3d(0, 0, 0);
    }

    100% {
      transform: translate3d(0, 30px, 0);
    }
  }

  #astronaut {
    animation: float linear 8.5s infinite alternate;
  }

  @media (prefers-reduced-motion: reduce) {
    #astronaut {
      @apply animate-none;
    }

    :global(#starfield) {
      @apply hidden;
    }

    #splash-bg-fallback {
      @apply block;
    }
  }

  .linear-gradient {
    border-image: linear-gradient(45deg, #818cf8, #c7d2fe, #bcee22 140%) 1;
  }
  .box {
    width: 200px;
    height: 50px;
    border-style: solid;
    border-width: 5px;
    border-radius: 100%;
  }

  .scroll-indicator {
    transition: transform 0.3s ease;
  }

  .scroll-indicator:hover {
    transform: translateY(-3px);
  }

  /* Animación para el icono único */
  @keyframes chevronBounce {
    0%, 100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(8px);
    }
  }

  :global(.chevron-single) {
    animation: chevronBounce 2s infinite ease-in-out;
  }

  :global(.chevron-single:hover) {
    animation-duration: 1s;
    transform: translateY(-2px);
  }
</style>


